# neuro-slop.ru

Статический сайт на Astro. Исходники — в `dev/`, собранный сайт лежит в корне репозитория и раздаётся GitHub Pages из ветки `main`.

## Рабочий цикл

```bash
cd dev
npm run dev     # локальная разработка
npm run build   # сборка в dev/dist + копирование результата в корень репозитория
```

Никогда не направлять `outDir` Astro в корень репозитория — Astro очищает выходную папку перед сборкой. Сборка идёт в `dev/dist`, скрипт `sync` копирует её в корень. Файлы сайта в корне (`index.html`, `_astro/`, `CNAME`, `.nojekyll`) — артефакты сборки, их не редактируют руками, но коммитят.

## Стиль: эстетика Apple

Сайт оформлен в визуальном языке apple.com — сдержанно, много воздуха, никакой пестроты. Все токены — в `dev/src/styles/global.css`, использовать их, а не сырые значения.

### Принципы

- **Системный шрифт SF**: стек начинается с `-apple-system` — на устройствах Apple рендерится San Francisco. Никаких подключаемых веб-шрифтов.
- **Плотная типографика**: крупные заголовки `font-weight: 600` с отрицательным `letter-spacing` (-0.015em у h1), базовый текст 17px / 1.47 / -0.022em.
- **Палитра** (светлая тема): текст `#1d1d1f`, вторичный текст `#6e6e73`, подложки `#f5f5f7`, акцент `#0071e3`, ссылки `#0066cc`. Тёмная тема — автоматически через `prefers-color-scheme`, цвета уже заданы в токенах.
- **Скругления**: карточки 18px (`--radius-card`), кнопки и бейджи — пилюли `980px` (`--radius-pill`).
- **Шапка**: липкая, полупрозрачная, с `backdrop-filter: saturate(180%) blur(20px)` — задана в `Layout.astro`, на новых страницах появляется сама.
- **Градиент-акцент**: для выделения ключевых слов в заголовках — градиент в духе Apple Intelligence (`108deg, #0894ff → #c959dd → #ff2e54 → #ff9004`) через `background-clip: text`. Использовать точечно, не на больших объёмах текста.

### Готовые классы

- `.button` — синяя кнопка-пилюля с hover-состоянием
- `.card` — карточка на подложке `var(--card-bg)` со скруглением 18px

### Чего не делать

- Не вводить новые цвета мимо токенов; вторичный текст — всегда `var(--text-secondary)`.
- Не забывать про тёмную тему: любые новые цвета добавлять в обе ветки `:root`.
- Не перегружать страницы — стиль держится на пустом пространстве и крупной типографике, а не на декоре.

Каждая новая страница (`dev/src/pages/*.astro`) оборачивается в `Layout.astro` — он подключает глобальные стили, шапку и футер.
