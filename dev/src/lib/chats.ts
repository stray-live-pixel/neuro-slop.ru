import { CHATS_SECTION } from './categories';

export { CHATS_SECTION };

// Публичный адрес тонкого сервера (Node + Caddy с авто-HTTPS на VPS).
// Это НЕ секрет — просто endpoint. Все секреты (ключ OpenRouter, системные
// промпты ботов, доступ к исходящему proxy) живут только на сервере в приватном
// репозитории и наружу не попадают. Чтобы сменить адрес (напр. на свой
// поддомен), задай PUBLIC_CHAT_API при сборке или поправь значение ниже.
export const CHAT_API_BASE = (
  import.meta.env.PUBLIC_CHAT_API?.toString() || 'https://77-105-170-239.sslip.io'
).replace(/\/$/, '');

// Дефолтные дисклеймеры безопасности — показываются под каждым чатом.
// Бот может переопределить их полем `disclaimers` в своём md-файле.
export const DEFAULT_DISCLAIMERS = [
  'Это шуточный развлекательный проект, а не совет. Решения принимаешь только ты.',
  'Не отправляй личные данные: имена, телефоны, адреса, пароли, рабочие секреты.',
  'Не используй для рабочих, юридических, медицинских и денежных решений.',
  'Отвечает ИИ — он может ошибаться и выдумывать. Относись с юмором.',
];

// Градиент бота: свой `accent` или, по умолчанию, градиент раздела.
export const accentOf = (accent?: string) => accent || CHATS_SECTION.gradient;
