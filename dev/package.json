{
  "name": "neuro-slop-ru",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && npm run sync",
    "sync": "rm -rf ../_astro && cp -R dist/. ..",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.0.0"
  }
}
